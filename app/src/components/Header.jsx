import React from "react";

const Header = props => {
  return (
    <div className="header">
      <div className="wrapper">
        <h2>Where in the world?</h2>
        <button className="btn btn-no-border">Dark mode</button>
      </div>
    </div>
  );
};

export default Header;
